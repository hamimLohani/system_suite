name: CI

on:
  push:
  pull_request:

jobs:
  smoke:
    name: Shell smoke tests
    runs-on: ${{ matrix.os }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run smoke tests
        env:
          TERM: dumb
        run: bash test/smoke.sh
